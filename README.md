# cli-cards-web

Browser-based editor for [cli-cards](https://github.com/catherinepereira/cli-cards). Design a terminal-style usage card by editing its title, colors, and command lines with a live preview, then download the result as a PNG.